#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
 
 
void solve(){
    ll n;
    scanf("%lld",&n);
    printf("%lld %lld\n", (n-1) * -1, n );
}
 
int main(){
    int T;
    scanf("%d", &T);
    while (T -- ) solve();
    // system("pause");
}
